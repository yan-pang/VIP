# 测试策略规范

> 默认保存为 `project/domains/<domain>/delivery/test/strategy.md`。  
> 与 `test/cases.md` 配套使用,先写策略再写详细用例。  
> 需求 / 规则编号以领域 `delivery/prd.md` 的 `P-XXX` 为主。

## 文档说明

- 对应 PRD:`project/domains/<domain>/delivery/prd.md`
- 对应设计:`project/domains/<domain>/design.md`
- 参考技术设计:`project/tech/` 下相关版本文件(按需,吸收依赖、接口约束、异常路径)
- 参考调研:`project/research/` 下相关版本文件(按需)

## 测试范围

### 本策略覆盖

| 模块 / 链路 | 测试重点 | 对应 P-XXX |
| --- | --- | --- |
|  |  |  |

### 本策略不覆盖

| 项目 | 不覆盖原因 |
| --- | --- |
|  |  |

## 分层思路

> 一句话说明本领域按什么维度分层(按模块 / 按规则 / 按角色 / 几种组合),以及选这个维度的理由。

- 本领域分层维度:
- 选择理由:

## 覆盖矩阵

> 这是这份文档的核心证据。每条 `P-XXX` 至少一个用例覆盖;关键规则至少一个正常路径 + 一个异常路径。

| P-XXX | 场景 | 正常 / 异常 / 恢复 | 优先级 | 备注 |
| --- | --- | --- | --- | --- |
| P-101 |  | 正常 | P0 |  |

## 优先级

| 优先级 | 划分标准 |
| --- | --- |
| P0 | 核心业务流程、数据准确性、权限安全 |
| P1 | 分支流程、关键校验、状态流转 |
| P2 | 边界、异常恢复、兼容性 |

## 回归范围

- 进回归集:所有 P0 用例、跨模块联动用例、历史缺陷相关用例
- 不进回归集:纯文案 / 纯样式、一次性初始化、已被更高优先级完全覆盖的冗余场景
- 本领域特殊回归点:

## 风险

| 风险项 | 风险描述 | 影响 | 对策 |
| --- | --- | --- | --- |
|  |  |  |  |

## 退出标准

- 所有关键 `P-XXX` 对应用例已执行并通过
- 高优先级问题已关闭或记录风险
- 回归集已整理
- `test/strategy.md` 与 `test/cases.md` 可互相回溯
