# 当前任务状态(单一事实源)

> 会话开局只读这一份即可掌握"现在在哪"。运行时状态集中在此,不要散进各领域 `design.md`。
> 决策历史见各领域 `domains/<domain>/decisions.md`;稳定设计见 `design.md`;整体路线见 `roadmap.md`。

## 当前主线

- V1 Mock 已完成一次跨代码 / 文档一致性整改：14 项产品边界均已确认并落实到实现、稳定设计与自动化契约测试。
- P0 领域:chat-workbench、player-center(核心)+ ops-admin、permission(基础)。
- P1：外部依赖对齐（企微会话存档 API / 阿里云 RPA 与无影云 / 飞书身份 / SCRM 互通）尚未进入生产集成。

## 领域状态清单

| 领域 | 阶段 | 说明 |
| --- | --- | --- |
| chat-workbench | 一致性整改完成 | 硬风控不可绕过；RPA 不可用进入可见待发队列并自动重试；结束会话只读且显式重新联系；首次成功前的主动会话为工作台临时态；撤回按本人 / 主管范围执行 |
| player-center | 一致性整改完成 | 多企业主体使用 `corpId + external_userid`；两种删好友状态都禁止发送；不可见关系不泄露精确数量；关系编辑使用版本校验 |
| ops-admin | 一致性整改完成 | 企微号只启停不删除；资源与历史保留；IP 基准取阿里云分配地址；常驻任务与发送批次术语拆分；服务层鉴权、审计和版本校验已补齐 |
| permission | 一致性整改完成 | 系统管理员管理全平台；角色说明三角色均可读；仅最后一名有效管理员受自降级 / 停用保护；注销与未登录态已补齐 |

## 最近动态(最多 5 条)

| 日期 | 领域 | 摘要 |
| --- | --- | --- |
| 2026-07-22 | 全部 P0 领域 | 完成跨域一致性整改：落实 14 项已确认边界，补服务层权限 / 乐观锁 / 风控队列 / 唯一身份键 / 可逆生命周期，并新增自动化契约测试与依赖安全检查。 |
| 2026-07-22 | chat-workbench / player-center | 需求方确认两个领域 V1 Mock 最终验收通过；至此四个 P0 领域全部完成验收，下一步先做项目整体 review,再逐领域产出 PRD 与测试用例。 |
| 2026-07-22 | player-center | 完成最终验收前收口：修正旧设计口径，统一工作台运行态；补齐列表联动、消息筛选 / 高亮、失败消息统计、权限可见性与不可用企微号拦截，并通过真实浏览器回归。 |
| 2026-07-22 | chat-workbench | 完成最终验收前收口：搜索改读工作台运行态，补齐标签与分类键盘交互；消息结果可自动展开旧轮次、滚动并高亮。动态消息、标签跳转和历史定位均已通过真实浏览器回归。 |

> 更早的动态与决策已归档到对应 `domains/<domain>/decisions.md`。

## 当前下一步

- **本轮验证已完成**：真实浏览器关键路径无控制台错误，`npm run check`（ESLint、8 条契约测试、TypeScript、生产构建）及依赖审计均通过。
- **生产前置**：补服务端持久化、鉴权与幂等、配置事件、外部 API 契约和真实审计留存；本地 Mock 不能替代这些能力。
- **版本交付**：用户明确要求发版时，从稳定设计、决策记录和测试结果生成 `project/delivery/v1.x/` 快照；当前不维护重复的领域 PRD 事实源。
