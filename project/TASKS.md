# 当前任务状态(单一事实源)

> 会话开局只读这一份即可掌握"现在在哪"。运行时状态集中在此,不要散进各领域 `design.md`。
> 决策历史见各领域 `domains/<domain>/decisions.md`;稳定设计见 `design.md`;整体路线见 `roadmap.md`。

## 当前主线

- V1 Mock 已完成一次跨代码 / 文档一致性整改：14 项产品边界均已确认并落实到实现、稳定设计与自动化契约测试。
- P0 领域:chat-workbench、player-center(核心)+ ops-admin、permission(基础)。
- P1：外部依赖对齐（企微会话存档 API / 阿里云 RPA 与无影云 / 飞书身份 / SCRM 互通）尚未进入生产集成。

## 领域状态清单

| 领域 | 阶段 | 说明 |
| --- | --- | --- |
| chat-workbench | 已按 PRD 反向对齐 | 以 11 章长期 PRD 为基线反向拉齐 design.md 与 `src/`：移除消息「待发送」态（依赖不可用直接失败）、落地回捞比对失败（已送达→失败）、移除撤回入口（P-117 暂缓仅只读）、企微号补「停用」态、会话标记改单选三值+清除、未读徽章收口（他人接待中不展示）、置顶上限 10→20、指派转接候选「在线→启用(active)」+转接弹窗当前负责人只读头、附件白名单、9.4 文案字典逐字对齐；`npm run check` 全绿 |
| player-center | 一致性整改完成 | 多企业主体使用 `corpId + external_userid`；两种删好友状态都禁止发送；不可见关系不泄露精确数量；关系编辑使用版本校验 |
| ops-admin | 交付边界已调整 | 正式拥有 `/control` 的投屏、单投屏互斥、人工接管、登录 / 退出与重启；企微号、资源、风控和审计规则保持同源 |
| permission | 一致性整改完成 | 系统管理员管理全平台；角色说明三角色均可读；仅最后一名有效管理员受自降级 / 停用保护；注销与未登录态已补齐 |

## 最近动态(最多 5 条)

| 日期 | 领域 | 摘要 |
| --- | --- | --- |
| 2026-07-24 | chat-workbench | 以 11 章长期 PRD 为基线**反向对齐** design.md 与 `src/`：移除消息「待发送/queued」态（临时依赖不可用直接失败、删自动重排队 useEffect）、落地回捞比对失败（已送达→失败，新增 FailureCategory + Mock 延迟翻转）、移除客服端撤回入口（P-117 暂缓仅只读 recalled 占位）、WechatAccountStatus 补 `disabled`、会话标记 tags[]→单值 tag+清除、未读徽章仅排队中/本人负责、置顶上限 10→20、候选「在线→启用(active)」+转接弹窗当前负责人只读头+指派并发冲突、附件白名单校验、9.4 文案字典逐字对齐；design.md 5 项 + decisions 同源；`npm run check`（lint+11 测试+build）全绿，真实浏览器关键路径无 error。 |
| 2026-07-23 | chat-workbench | 彻底移除「待发送」消息状态：结果状态机只剩 发送中/已送达/失败/历史已撤回。所有发送前临时执行依赖不可用（云电脑/机器人不可用、RPA 积压/限流）一律直接失败、由客服手动重发，系统不排队自动重发；系统自动重试仅保留「结果未知」（网络/超时/无回执，保持发送中幂等补发）。临时草稿/重新联系首条刷新保留规则删去“待发送持久化+自动重试”分支（首条未成功即刷新清除）。design.md §4、prd.md 6.2/6.3/6.6/6.8+P-103/114/115/117/118/120+附录、decisions 同源修正。 |
| 2026-07-23 | chat-workbench | 收束 6.2 会话状态转换矩阵「临时草稿」口径：明确临时草稿为仅工作台可见、不入 3 态模型 / 外部索引的前置态；草稿期负责人由“当前客服（延迟落地）”改为“不写入（延迟到首条成功才指派）”；失败结果拆分“待发（本机持久化、刷新保留、自动重试）”与“首条失败 / 未发送（未落库、刷新清除）”。同步 design.md 图 1 要点与 decisions。 |
| 2026-07-23 | chat-workbench | 长期 PRD 补强：先在 Design/decisions 锁定 9 项业务事实（撤回暂缓、违禁词失败气泡+清空编辑区且不支持重新编辑恢复原文、标记单选三值、实际可见后已读、临时会话刷新保留规则、好友异常不改正式状态、账号离线 vs 临时依赖区分），升级 external-prd 生成规范至 11 章+元素规格模板+提示优先级+文案字典，再整篇重生成 delivery/prd.md；R045/P-117 暂缓已同步 roadmap 与权限矩阵。 |

> 更早的动态与决策已归档到对应 `domains/<domain>/decisions.md`。

## 当前下一步

- **本轮验证已完成**：真实浏览器关键路径无控制台错误，`npm run check`（ESLint、8 条契约测试、TypeScript、生产构建）及依赖审计均通过。
- **生产前置**：补服务端持久化、鉴权与幂等、配置事件、外部 API 契约和真实审计留存；本地 Mock 不能替代这些能力。
- **长期 PRD**：下一步按同一生成规格依次完成 ops-admin、player-center 和 permission；每次均以对应 `design.md` 为事实源并恢复历史 P/R 编号。
- **版本交付**：四个领域长期 PRD 和测试追溯全部完成后，再统一生成 `project/delivery/v1.x/` 版本快照。
