# 项目品牌规范

> 默认保存为 `project/ui-brand.md`。  
> 这是一份项目级默认设计规范；所有领域的 `design.md` 默认继承这里。只有领域特有的例外才写回对应领域文档。

## 使用说明

- 这份文档只维护项目级、长期稳定的视觉与品牌规则。
- 领域业务规则、页面流程和功能细节写到 `project/domains/<domain>/design.md`。
- 如果只是某个页面临时偏离默认规范，在对应 `design.md` 中写“设计例外说明”，不要回写这里。
- 如果某项尚未确定，可暂时沿用 starter 默认值，但应优先在本文件补齐，不要分散到多个领域文档。
- 「当前 starter 默认值」一节只是模板首次起步时的占位，第一个真实项目初始化时应按实际品牌覆盖或清空。

## 当前 starter 默认值

- 主色：`#2364aa`
- 信息色 / 链接色：`#2364aa`
- 默认圆角：`12px`
- 语言基调：中文优先
- 页面气质：专业、克制、偏 B 端工具型
- 默认页面类型：搜索 + 列表 + 分页 + 弹窗

## 品牌基础

- 品牌名称：
- 品牌关键词：
- 目标气质：
- 适用范围：

## 色彩规范

### 品牌色

- 主色：
- 主色浅阶：
- 主色深阶：
- 辅助色：
- 强调色：

### 语义色

- 成功：
- 警告：
- 错误：
- 信息：

### 中性色

- 主文本：
- 次文本：
- 辅助文本：
- 一级边框：
- 二级边框：
- 页面底色：
- 卡片底色：

## 字体与排版

- 标题字体：
- 正文字体：
- 代码或数字字体：
- 主标题字号：
- 次级标题字号：
- 正文字号：
- 辅助字号：
- 默认字重：
- 默认行高：

## 形状与密度

- 默认圆角：
- 轻量阴影：
- 强调阴影：
- 页面内容区密度：宽松 / 中等 / 紧凑
- 组件内部间距：
- 页面区块间距：

## 组件气质与默认做法

- 导航与菜单：
- 列表与表格：
- 表单：
- 弹窗 / 抽屉：
- 标签页：
- 卡片：
- 数据展示：

## 页面氛围

- 背景基调：
- 视觉层级：
- 强调信息如何表达：
- 弱化信息如何表达：

## 禁用样式

- 不要使用的颜色：
- 不要使用的布局倾向：
- 不要使用的组件风格：
- 不要出现的品牌冲突：

## 例外原则

- 允许在领域 `design.md` 中声明例外的场景：
- 例外说明应包含：
